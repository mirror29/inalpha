# Inalpha 操作者控制台 · 可选的本地覆盖
#
# 注意:控制台默认**直接读仓库根的 .env**(见 next.config.ts 的 loadRootEnv),
# 后端 service URL + JWT_SECRET 都从那继承,正常情况下你**不需要**建这个文件。
#
# 只在想局部覆盖时才把本文件复制为 .env.local(优先级高于根 .env)。常见用途:

# ── 切换控制台身份(后端按 sub 派生稳定 account_id)──
# CONSOLE_SUBJECT=console:dev
# CONSOLE_EMAIL=console@inalpha.dev

# ── 指向远端 / 非默认端口的后端 ──
# PAPER_SERVICE_URL=http://127.0.0.1:8002
# DATA_SERVICE_URL=http://127.0.0.1:8001
# RESEARCH_SERVICE_URL=http://127.0.0.1:8003
# MASTRA_URL=http://127.0.0.1:4111

# ── 覆盖鉴权(必须与后端一致;后端只接受 HS256/384/512)──
# JWT_SECRET=
# JWT_ALGORITHM=HS256
