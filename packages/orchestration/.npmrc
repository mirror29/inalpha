# pnpm 11 的"运行脚本前自动 pnpm install"会把 ERR_PNPM_IGNORED_BUILDS 当 fatal。
verify-deps-before-run=false

# 不要把 esbuild/protobufjs 的 build script 当致命错误（package.json 已 allowlist）
ignored-built-dependencies-warning=false
